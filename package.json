{
  "name": "workspace",
  "version": "1.0.0",
  "private": true,
  "license": "MIT",

  "scripts": {
    "preinstall": "sh -c 'rm -f package-lock.json yarn.lock; case \"$npm_config_user_agent\" in pnpm/*) ;; *) echo \"Use pnpm instead\" >&2; exit 1 ;; esac'",

    "build": "tsc",
    "start": "node dist/artifacts/api-server/src/index.js",
    "dev": "ts-node artifacts/api-server/src/index.ts",

    "typecheck": "tsc --noEmit"
  },

  "dependencies": {
    "express": "^4.19.2"
  },

  "devDependencies": {
    "typescript": "~5.9.2",
    "ts-node": "^10.9.1",
    "@types/node": "^20.11.30",
    "prettier": "^3.8.1"
  }
}
